import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "🎵 Conversor YouTube para MP3 - Python Desktop App",
  description: "Aplicação desktop Python com interface Tkinter para converter vídeos do YouTube em arquivos MP3 de alta qualidade. Usa yt-dlp e FFmpeg.",
  keywords: ["YouTube", "MP3", "Conversor", "Python", "Tkinter", "yt-dlp", "FFmpeg", "Download"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Conversor YouTube para MP3",
    description: "Python desktop app para converter vídeos do YouTube em MP3",
    url: "https://chat.z.ai",
    siteName: "Z.ai",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Conversor YouTube para MP3",
    description: "Python desktop app para converter vídeos do YouTube em MP3",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
